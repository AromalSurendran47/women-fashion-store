# Base URL of the Aura backend API (the Express server in ../backend).
# When set, the frontend fetches live data from the backend and falls back to
# bundled dummy data if the API is unreachable.
# Leave empty to run purely on local dummy data.
NEXT_PUBLIC_API_URL="http://localhost:5000/api"
